{
  "permissions": {
    "allow": [
      "Bash(npm install *)",
      "Bash(ls -d node_modules/.next-* node_modules/.*-* 2>/dev/null; echo \"--- cleaning stale temp dirs ---\"; rm -rf node_modules/.next-* node_modules/.*-[A-Za-z0-9]* 2>/dev/null; echo done)",
      "Bash(xargs -r rm -rf)",
      "Bash(rm -rf node_modules && npm install 2>&1 | tail -15)",
      "Bash(npx prettier *)",
      "Bash(npm pkg *)"
    ],
    "additionalDirectories": [
      "/home/jonel/portfolio/node_modules",
      "/home/jonel/portfolio"
    ]
  }
}
